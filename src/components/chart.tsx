export const BarChart = ({ data, index, categories, colors, valueFormatter, className }: any) => {
  return <div className={className}></div>
}

export const LineChart = ({ data, index, categories, colors, valueFormatter, className }: any) => {
  return <div className={className}></div>
}

export const PieChart = ({ data, index, category, valueFormatter, className }: any) => {
  return <div className={className}></div>
}

